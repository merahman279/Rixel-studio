// ============================================================================
// Rixel Studio — Public Client Runtime Configuration
// ============================================================================
// Safe public configuration only. Secret keys (service_role) must NEVER be put here.
// You can set your Supabase publishable anon key here, or enter it via the
// Admin Login UI ("Save & Connect"), which saves it securely in browser localStorage.
// ============================================================================
window.ENV = window.ENV || {};
window.ENV.SUPABASE_URL = window.ENV.SUPABASE_URL || "https://telynaezwbibshzckokh.supabase.co";
window.ENV.SUPABASE_ANON_KEY = window.ENV.SUPABASE_ANON_KEY || "";
